type: group
fields:
  securedPageEnabled:
    type: toggle
    label:
      en: Page Protection
      de: Seitensichtbarkeit
    text:
      en: Password protection this page?
      de: Seite mit Passwort schützen?

  securedPageRoles:
    label:
      en: Roles
      de: Rollen
    type: multiselect
    options: query
    when:
      securedPageEnabled: true
    query:
      fetch: kirby.roles
      text: "{{ item.title }}"
      value: "{{ item.id }}"
